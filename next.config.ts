import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // output: 'export', // <- این خط رو حذف کن
  // basePath: '/something', // <- این خط رو حذف کن
};

export default nextConfig;
